rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow only authenticated users to access documents in "gsSyncData"
    match /gsSyncData/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
